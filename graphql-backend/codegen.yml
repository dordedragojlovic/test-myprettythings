overwrite: true
schema: "src/types/index.ts"
documents: null
generates:
  src/generated/graphql-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      useIndexSignature: true
